
  # AMU CONNECT

  This is a code bundle for AMU CONNECT. The original project is available at https://www.figma.com/design/aitbpk0cDfbrvrEyGlUfu0/AMU-CONNECT.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  